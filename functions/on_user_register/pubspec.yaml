name: on_user_register
description: >
  Appwrite Cloud Function — provisions profiles, user_roles, buildings, channels,
  and user_apartments when a user updates prefs after signup (users.*.update.prefs).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  dart_appwrite: ^23.0.0
